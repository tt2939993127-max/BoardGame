## 1. 建模与迁移设计
- [ ] 1.1 盘点 DiceThrone 当前所有掷骰入口：主骰、目标掷骰、防御骰、`rollDie`、奖励骰、闪避投骰、对掷 / 比骰、Ultimate 结算骰。
- [ ] 1.2 为每类掷骰定义当前上下文元数据：归属玩家、目标玩家、所属阶段、来源、可否改骰、可否重掷、合法干预者、是否 Ultimate 锁定、结算方式。
- [ ] 1.3 明确哪些现有 `displayOnly` 场景必须迁入当前骰子上下文，哪些仍可保留为纯展示回放。
- [ ] 1.4 定义“任一时刻只有一个当前骰区”的领域不变量，并写清新投掷破坏性覆盖旧投掷、旧骰面覆盖前提交为结算输入、覆盖后只能步骤级回退的流程。

## 2. 领域层重构
- [x] 2.1 新增 `currentRollContext` 类型、创建 / 更新 / 清空 helper、唯一读取入口与兼容读取层。
- [ ] 2.2 将 `getActiveDice`、`getRollerId`、骰子签名、响应窗口源 ID、可改骰判断改为优先读取当前上下文。
- [x] 2.3 将 `MODIFY_DIE` / `REROLL_DIE` / 战术家与其他 `rerollDie` 被动重构为面向当前上下文，不再只绑定 `core.dice`。
- [ ] 2.4 将通用改骰 / 重掷卡牌交互绑定到当前上下文，并从 `context.policy` 判断合法操作者。
- [ ] 2.5 将目标掷骰、闪避投骰、`rollDie` 效果、规则可改奖励骰、对掷 / 比骰逐批迁入当前上下文。
- [ ] 2.6 将可交互额外骰从“立即 displayOnly + immediate resolution”拆为“创建当前上下文 -> 等待干预 / 跳过 -> 最终骰面结算”。
- [ ] 2.7 为 Ultimate 成功发动后的结算骰增加不可修改策略，并补齐命令级错误口径。
- [ ] 2.8 保持 Targeting Roll、attack bonus、threshold effect、direct damage、token negate 等现有结算语义在新模型下不回归。

## 3. 结算帧与流程恢复
- [ ] 3.1 接入 DiceThrone 结算帧：当前骰子未结算时阻塞阶段推进。
- [ ] 3.2 支持中途出牌 / 使用被动 / 使用 Token 后，如果新投掷覆盖了旧骰区，可回退到覆盖前对应步骤再继续游戏。
- [ ] 3.3 禁止使用通用 UndoSystem 承担骰子结算恢复；仅在真实玩家撤回整局动作时使用撤回。
- [ ] 3.4 迁移 `pendingBonusDiceSettlement` 为兼容适配或展示派生数据，避免继续成为第二个当前骰子源。

## 4. UI 与 AI
- [ ] 4.1 统一 DiceTray / BonusDieOverlay / BoardOverlays / 卡牌特写对当前骰子上下文的消费。
- [ ] 4.2 明确 UI 只能展示一个当前可操作骰区；其它骰子结果只能是已提交结算输入、历史摘要或已结算回放。
- [ ] 4.3 更新被动能力、通用骰子卡、AI 行动枚举与权重逻辑，使其可识别当前上下文中的额外骰、闪避骰和目标骰。
- [ ] 4.4 为多人局与 2v2 显示当前骰子的拥有者、目标与可干预边界。

## 5. 验证
- [x] 5.1 补齐领域测试：任一时刻最多只有一个当前骰区，且新投掷会覆盖旧投掷。
- [ ] 5.2 补齐领域测试：战术家战术优势可在合法当前骰子上下文中重掷，包括闪避 / 额外骰等非主骰场景。
- [ ] 5.3 补齐领域测试：闪避投骰可进入当前上下文，最终骰面决定免伤或失败。
- [ ] 5.4 补齐领域测试：目标掷骰、额外技能骰、奖励骰重掷、对掷 / 比骰、Ultimate 锁定分别不回归。
- [ ] 5.5 补齐领域测试：中途出牌 / 被动 / Token 响应产生新投掷时会覆盖旧骰区；需要改旧骰面时回退到覆盖前步骤，而不是使用整局撤回。
- [ ] 5.6 补齐 UI / E2E 证据：至少覆盖“可改骰额外掷骰”“闪避投骰可干预”“Ultimate 锁定额外骰”三条真实链路。
- [ ] 5.7 产出 evidence 文档，逐项说明哪些掷骰已从特效链迁移到统一当前骰子上下文。
