## 1. Approval

- [x] 1.1 建立独立 change，只覆盖 `1908` 原力之握。
- [x] 1.2 明确保留现有束缚关系、生命周期清理和 `2224` 魔物路径。

## 2. Config Package Source And Loader

- [x] 2.1 扩展结构化授予特性枚举，严格读取 `restrained`。
- [x] 2.2 为 `1908` 录入可见附属结界语义并标记为已实现。

## 3. Runtime Migration

- [x] 3.1 接入 `1908` 的不羁目标校验。
- [x] 3.2 从配置授予特性生成现有 `ARENA_OBJECT_RESTRAINED` 关系事件。
- [x] 3.3 保持移动、推斥、传送和结界销毁清理的既有 owner 不变。

## 4. Verification

- [x] 4.1 增加 `1908` 配置语义和 `requiresCodeSupport` 回归测试。
- [x] 4.2 增加合法施放建立束缚来源、移除展示文案后仍生效的领域流程测试。
- [x] 4.3 增加不羁目标拒绝、移动 / 推斥 / 传送拒绝和驱散后清理回归测试。
- [x] 4.4 运行 Mage Wars / game-config 测试、ESLint、TypeScript 和 OpenSpec 严格校验。

## 5. Explicitly Deferred

- [x] 5.1 不在本 change 中实现 `1904`、`1912`、庇护、区域结界、隐藏结界、展示 / 反制或其它结界触发能力。
