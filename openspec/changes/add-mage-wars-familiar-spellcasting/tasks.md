## 0. Approval

- [x] 0.1 用户批准魔宠 / 再生点独立施法提案，并持续授权继续完成本提案。

## 1. Data And Contracts

- [x] 1.1 补齐 `2908 乌鸦魔宠胡金`、`2218 巢穴` 的费用、类型、等级、行动 / 施法限制、聚魔 / 法力字段、规则文本与 atlas/frame 合同，见 `docs/games/mage-wars/rule/familiar-spellcasting-card-contract.md`。
- [x] 1.2 逐卡核对并裁定 `2905`、`3017`：字段已核读，但本批保持 deferred，不进入运行时配置，裁定见来源卡字段合同。
- [x] 1.3 扩展配置包校验，拒绝缺少来源类型、法术类型限制或非法阶段的施法来源。

## 2. Domain Runtime

- [x] 2.1 为场上对象增加对象法力和独立单卡计划状态，保留玩家法师计划槽语义不变。
- [x] 2.2 增加对象计划命令 / 事件 / reducer，校验每个对象最多一张、来源限制和玩家法术书归属。
- [x] 2.3 扩展施法命令显式指定施法对象，按魔宠行动阶段 / 再生点部署阶段区分入口。
- [x] 2.4 实现对象法力优先支付、法师法力补足、范围从对象位置计算和行动消耗。
- [x] 2.5 实现对象被摧毁、法术被反制 / 取消、法术成功结算和回合结束返还的计划牌生命周期。
- [x] 2.6 让 `1804` 只消费真实对象成功结算事件，并与来源失效、法师施法、普通生物和多来源边界一致。

## 3. Verification

- [x] 3.1 覆盖魔宠 / 再生点计划、阶段限制、对象范围和对象优先支付。
- [x] 3.2 覆盖反制 / 取消退款、来源先被摧毁、未施放返还和成功结算后的 `1804`。
- [x] 3.3 覆盖 player view 隐藏对象计划牌，只显示卡背 / 数量。
- [x] 3.4 运行 Mage Wars 定向测试、TypeScript、ESLint 和 OpenSpec 严格校验；定向测试 25 项、Mage Wars 全量 257 项、TypeScript、Mage Wars 定向 ESLint 和 OpenSpec 严格校验均通过。
- [x] 3.5 完成首批正式来源图集素材链检查：复用 deck `22/29` 已有 `4096x3294` 源图 / WebP，加入 `2218` / `2908` frame、配置包资产引用和 `atlas-configs` manifest 更新；增量 manifest 校验、资源路径和 CardPreview 回归通过。完整 `--full` 镜像校验不适用于当前含远端保留键的共享 atlas manifest。

## 4. Explicitly Deferred

- [x] 4.1 本批不接入已核读但延后的 `2905`、`3017`；它们保持 source-field-locked / deferred，待后续批次单独裁定。
- [x] 4.2 本批不扩展完整 322 张法术、自由构筑、四人模式、完整 AI、教程和行动日志 UI。
