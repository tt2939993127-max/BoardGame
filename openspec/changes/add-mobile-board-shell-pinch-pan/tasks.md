## 1. 设计与契约

- [ ] 1.1 为 manifest 增加显式战场缩放所有权字段，并补运行时归一化与注册表测试
- [ ] 1.2 明确 board-shell 通用 `pinch + pan` 的启用条件、排除规则、边界与重置行为

## 2. 框架实现

- [ ] 2.1 新增通用移动战场手势状态管理（双指缩放、平移、边界约束、重置）
- [ ] 2.2 将该能力接入 `MobileBoardShell` 主画布层，确保 HUD / sideDock / bottomRail 不被一起缩放
- [ ] 2.3 保证默认单指交互不被通用层接管；仅在进入缩放态后承接平移

## 3. 游戏接入

- [ ] 3.1 将 `smashup` 标记为首批 `shell-pinch-pan` 接入游戏
- [ ] 3.2 将已拥有整块棋盘/战场放大能力的游戏标记为 `game-owned`
- [ ] 3.3 对其他 `board-shell` 游戏逐个审查是否适合后续启用，不默认批量打开

## 4. 验证

- [ ] 4.1 补单元测试覆盖 manifest 归一化与 `MobileBoardShell` 能力开关
- [ ] 4.2 补 E2E，验证移动端双指缩放后可单指拖拽平移，且不影响主 HUD
- [ ] 4.3 补 E2E，验证已标记 `game-owned` 的游戏不会被通用缩放层接管
- [ ] 4.4 输出 evidence，包含 PC 对照图、移动端默认态图、缩放态图和只读/交互安全结论
