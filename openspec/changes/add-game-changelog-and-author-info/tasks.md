## 1. 后端数据与权限
- [ ] 1.1 扩展用户模型与用户详情 DTO，新增 `adminPermissions.gameChangelog.mode/gameIds`，缺省按 `all` 回退。
- [ ] 1.2 新建游戏更新日志模块（schema / dto / service / controller），支持按 `gameId` 过滤与排序。
- [ ] 1.3 实现公开接口，仅返回某个游戏的已发布更新日志。
- [ ] 1.4 实现后台 CRUD（新建、编辑、发布、撤回发布、删除）并在写接口中校验管理员的游戏范围权限。
- [ ] 1.5 为公开过滤、发布状态切换、权限拒绝与权限保存补 API 测试。

## 2. 后台管理界面
- [ ] 2.1 在后台侧边栏新增“更新日志”入口，并复用现有 admin 布局与表单风格。
- [ ] 2.2 实现按游戏筛选的更新日志管理页，支持草稿/已发布状态切换。
- [ ] 2.3 在 `src/pages/admin/UserDetail.tsx` 增加“更新日志权限”设置区，支持“全部游戏 / 指定游戏”。
- [ ] 2.4 为后台更新日志页和权限设置区补关键交互测试。

## 3. 游戏注册与作者内容注入
- [ ] 3.1 扩展 `GameManifestEntry`，增加轻量作者名称字段。
- [ ] 3.2 扩展 `GameClientManifestEntry`，增加可选的 `loadAuthorInfo` 契约。
- [ ] 3.3 更新 `scripts/game/generate_game_manifests.js`，自动发现 `src/games/<gameId>/author.tsx` 并生成懒加载入口。
- [ ] 3.4 至少为一个现有游戏补示例 `author.tsx`，并验证无该文件的游戏会走“佚名 + 占位内容”回退。

## 4. 前台游戏详情弹窗
- [ ] 4.1 放大 `GameDetailsModal` 桌面端宽度，保证排行榜 + 更新日志双栏不裁切。
- [ ] 4.2 在教程按钮上方增加作者名称与 `i` 入口，并接入作者详情弹窗。
- [ ] 4.3 在排行榜 tab 中实现“左排行榜 / 右更新日志”的桌面布局和移动端纵向堆叠布局。
- [ ] 4.4 接入已发布更新日志查询、空态展示与错误态展示，保持房间与评论 tab 行为不变。
- [ ] 4.5 为作者回退、作者弹窗、更新日志空态、双栏布局补前端测试。

## 5. 文档与收尾
- [ ] 5.1 更新相关文档，说明 `author.tsx` 约定、`authorName` 字段和后台更新日志发布流程。
- [ ] 5.2 补齐 `lobby` / `admin` / `common` 相关文案。
- [ ] 5.3 实现阶段完成后运行相关 lint、测试与 OpenSpec 校验。
