## 0. Approval

- [x] 0.1 Approval Gate: 用户已于 2026-07-26 明确批准 `add-mage-wars-foundation` 的 foundation 范围；批准边界为首轮基础版/学徒闭环，不包含全 322 张法术、自由构筑、四人模式、豪华竞技场或完整附加能力。

## 1. Stage 0 Intake

- [x] 1.1 建立来源索引：规则 PDF、FAQ/勘误、Markdown 导出、Workshop JSON、图片/模型目录。
- [x] 1.2 建立规则 intake 摘要：基础范围、阶段、动作、攻击、施法、隐藏信息和 FAQ 边界。
- [x] 1.3 建立规则对象素材矩阵，覆盖基础版主要对象并保留 blocked 状态。
- [x] 1.4 建立素材候选审计，记录 Workshop 对象数量、图片尺寸组、候选组和准入状态。
- [x] 1.5 建立布局真相源合同，区分 TTS 结构参考与最终 UI 风格。
- [x] 1.6 建立需求对齐表，记录用户对新游戏流程、Phaser 和法术特效的明确口径。
- [x] 1.7 建立 `Workshop URL -> 本地文件` 匹配表，优先覆盖标准竞技场、法师牌、学徒法术书、骰子和标记。
- [x] 1.8 为首轮运行时对象产出正式语义命名、落点、压缩/manifest 计划；未批准前不移动资源。

## 2. OpenSpec Foundation

- [x] 2.1 创建 `add-mage-wars-foundation` proposal。
- [x] 2.2 创建 foundation design，包含架构、FX 和范围边界。
- [x] 2.3 创建 `mage-wars` spec delta。
- [x] 2.4 运行 `openspec validate add-mage-wars-foundation --strict --no-interactive` 并修复格式问题。

## 3. Post-Approval Implementation Plan

- [x] 3.1 重新建立 UI 设计前置合同：原 `board-ui-preview.html` / `board-ui-preview-audit.md` 已被用户否决，原因是设计前未重新读取规则真相源，且应由正式素材承载的对象被 HTML/CSS、临时裁图或未正式落盘素材替代；已补规则/素材前置矩阵和设计目录状态，旧预览不得恢复为验收图。
- [x] 3.1.1 补设计验收门禁：AI 图面核验前必须先确认规则真相源、规则对象素材矩阵、正式素材状态和替代批准均通过；已回写 `.spec/skills/boardgame-ui-imagegen/SKILL.md`、`.spec/skills/create-new-game/SKILL.md` 和 `references/ui-implementation-gates.md`，当前 `board-ui-preview.html` 与 `temp/mage-wars/design-audit/board-ui-preview-1920x1080.png` 只保留为失败证据，不得打开给用户人工验收，也不得作为实现入口。
- [x] 3.1.2 补主 UI 设计前置矩阵：已重新读取 `docs/games/mage-wars/rule/**`、`docs/games/mage-wars/intake/runtime-asset-plan.md` 和正式素材状态，新增 `docs/games/mage-wars/design/implementable/board-ui-preflight-matrix.md`，列出“规则对象 -> 应用素材 -> 当前状态 -> 画面表达 -> 是否允许出现”矩阵；标准竞技场、四名法师、通用卡背、91 张学徒法术牌、攻击骰贴图、就绪 / 伤害 / 基础状态 token 已达到 `design-asset-ready`，主要首屏 / 预览资源已接入 `criticalImageResolver` 与 `CardPreview`。该条记录的是当时设计稿尚未生成的前置状态；当前有效候选已由 3.1.3 的 Open Design v6 artifact 覆盖。
- [x] 3.1.2a 回代同稿同源规范：用户指出“设计前没看规则、该用素材处没用素材”后，已补 `.spec/skills/boardgame-ui-imagegen/SKILL.md`、`.spec/skills/create-new-game/references/ui-implementation-gates.md`、`.spec/knowledge/README.md` 和 `docs/games/mage-wars/design/README.md`；下一版任何 prompt / 预览 / 图面审计 / 人工验收候选都必须自带本轮规则读取账本和可见主体素材账本，且正式素材必须实际参与图面，不能只被文字、CSS、临时裁图或边框壳替代。
- [x] 3.1.2b 回代设计事故拦截：用户继续指出“没看规则、没用素材、边框过重”属于规范问题后，已补项目 UI 生图 skill、UI 实现门禁、文档索引和 Mage Wars 设计索引；当前失败稿必须降为 `failed-candidate`，下一版先交设计前置包，且“已读规则 / 已用素材”必须能回到本轮规则页段、正式素材像素来源和边框职责清单。
- [x] 3.1.2c 收紧规则 / 素材证据：已补 `.spec/skills/boardgame-ui-imagegen/SKILL.md`、`.spec/skills/create-new-game/references/ui-implementation-gates.md`、`.spec/knowledge/README.md`、`docs/games/mage-wars/design/README.md` 和 `docs/games/mage-wars/design/reference/step1-runtime-board-imagegen-brief.md`；下一版候选必须把每条关键规则结论映射到画面主体和设计决策，并为每个主体素材记录路径存在、尺寸、atlas frame 命中或来源锁定程序化证据。
- [x] 3.1.2d 回代正式素材输入包门禁：用户指出“该用素材的地方没用素材”后，已补 `.spec/skills/boardgame-ui-imagegen/SKILL.md`、`.spec/skills/create-new-game/references/ui-implementation-gates.md`、`.spec/knowledge/README.md`、`docs/games/mage-wars/design/README.md` 和 `docs/games/mage-wars/design/reference/step1-runtime-board-imagegen-brief.md`；下一版位图设计稿生成前必须先创建素材输入包，证明 `正式资源 -> 输入包 -> 生成输入 -> 图面主体` 连通；只在 prompt 写路径、用边框 / 面板 / 文字壳或临时图承载主体时直接判 `REVISE`。
- [x] 3.1.2e 回代出图前硬回执：用户继续指出“没看规则、没用素材”说明规范还不足后，已补 `.spec/skills/boardgame-ui-imagegen/SKILL.md`、`.spec/skills/create-new-game/references/ui-implementation-gates.md`、`docs/games/mage-wars/design/README.md` 和 `docs/games/mage-wars/design/reference/step1-runtime-board-imagegen-brief.md`；下一版生成命令前必须先写出本轮规则读取、规则到画面映射、正式素材进入生成输入、框体职责和人工验收状态五项硬回执，缺任一项不得生成、不得 AI PASS、不得打开给用户。
- [x] 3.1.2f 记录正式出图链阻塞：已重新执行 Open Design `codex-gpt-image-2` 生成任务 `bbf3988c-836c-46f8-a114-12ae6e22633b`，任务失败于 Codex CLI 未写出 `$CODEX_HOME/generated_images/**/ig_*` 或 `call_*` 图片；随后执行 API-backed `gpt-image-2` 任务 `f9174ecd-d17c-4bda-821e-c9521620e7cf`，Open Design 能识别本机 Codex auth 中的 OpenAI provider，但本机直连外网与 OpenAI API 均超时，临时走本机代理 `127.0.0.1:17890` 可触达 OpenAI 但当前 API key 返回 401；已新增 `docs/games/mage-wars/design/generated/step1-runtime-board-v1-generation-blocker.md`。该阻塞只保留为历史 media / imagegen 路线记录；当前有效设计路线已改为 Open Design v6 artifact。
- [x] 3.1.2g 回代设计启动门禁：用户指出“没看规则就设计、该用素材处没用素材”说明拦截点不能只放在出图前；已补全局 `ui-design-pipeline`、项目 `.spec/skills/boardgame-ui-imagegen/SKILL.md`、新游戏 UI 门禁和 Mage Wars 设计索引，明确 Design I/O 声明、Open Design brief、prompt、HTML 预览、风格合同和实现骨架都属于设计动作，形成布局 / 主次 / 风格判断前必须先完成本轮规则重读、规则到画面映射、正式素材账本和框体职责。
- [x] 3.1.2h 复测正式出图链：临时启动带 `HTTP_PROXY` / `HTTPS_PROXY=http://127.0.0.1:17890` 的 Open Design daemon `7458`，确认代理路径可访问外网 / OpenAI；使用同一 reference sheet 和 prompt 执行 `gpt-image-2` 任务 `be202553-a15a-4bb2-adb5-6887fad5b64d`，失败原因为 `openai 401 / Incorrect API key`；临时 daemon 已停止，`step1-runtime-board-v1.png` 未生成。该结论只适用于历史 media / imagegen 路线；当前 Open Design v6 artifact 路线已通过 AI 图面核验并允许人工验收。
- [x] 3.1.2i 回代 Open Design 入口分层：用户指出“没看规则就设计、该用素材处没用素材”说明工具入口也会误导为设计稿完成；已补 `docs/infra/open-design.md` 与 `.spec/knowledge/README.md`，明确 Open Design 只是设计工具入口，不等于设计稿合格；设计稿交付仍必须是 PNG / JPG / WebP 位图，并在 Open Design 运行前完成本轮规则重读、正式素材输入包、出图前硬回执和 AI 图面核验，不能用 artifact、HTML 预览、运行页截图或 reference sheet 替代。
- [x] 3.1.2j 回代 imagegen 直出拦截：用户再次指出“没看规则就设计、该用素材的地方没用素材”说明只在项目设计 skill 里写门禁仍不够；已补全局 `D:\codex-home\skills\.system\imagegen\SKILL.md`，要求规则 / 业务 / 素材驱动的 UI、游戏画面、桌面布局或产品流程在调用生图前必须先有本轮规则读取、三条以上会改变画面的结论、逐主体素材账本和真实图像输入 / reference sheet；只在 prompt 写路径或素材名不算用素材，缺证据时只能产出 blocked brief / 缺口清单，不得生成完成态设计稿。
- [x] 3.1.2k 回代少框体默认修法：用户指出“该用素材处没用素材、边框过重”后，已清理项目 `boardgame-ui-imagegen` 中“明确边框”作为可实施表达的残留暗示；下一版 Mage Wars 主 UI 关键信息默认用贴对象轻量高亮、素材内徽记、状态色块、实体令牌、原卡槽标记或必要按钮态表达，不能把“加一圈边框”作为默认修法。
- [x] 3.1.2l 回代根入口与主仓 skill：用户继续指出“没看规则就设计、该用素材处没用素材”说明主仓根入口和主仓项目 skill 仍可能绕过 worktree 内门禁；已同步根 `AGENTS.md` 的 Open Design 口径、`.spec/knowledge/README.md` 的“新游戏位图设计稿 / 设计批准门禁”、主仓 `.spec/skills/boardgame-ui-imagegen/SKILL.md` 和 `.spec/skills/create-new-game/references/ui-implementation-gates.md`，要求出图 / Open Design / HTML 预览 / AI 图面核验 / 人工验收前必须有本轮规则重读、规则到画面映射、正式素材输入包、出图前硬回执和 AI PASS；缺任一项只能阻塞或输出缺口清单，不得继续沿旧稿调样式。
- [x] 3.1.2m 回代动作证据门禁：用户再次指出“没看规则就设计、该用素材的地方没用素材”说明不能只靠文档列路径；已同步全局 `ui-design-pipeline`、全局 `imagegen`、主仓 / worktree 项目 UI skill、UI 实现门禁、文档索引和 Mage Wars 设计索引，要求规则读取能回查到当前轮次实际打开真相源 / 素材合同 / 输入包，素材使用必须能证明进入图像输入、拼接基底、reference sheet、atlas crop 或运行时渲染来源；只写 prompt、复述摘要、素材角标化或边框 / 文字壳承载主体时仍判 `REVISE`。
- [x] 3.1.2n 回代启动自证门禁：用户指出“没看规则就设计、该用素材的地方没用素材”说明规范仍会被当作背景文本跳过；已补全局 `ui-design-pipeline`、全局 `imagegen`、主仓 / worktree 项目 UI 生图 skill 和 `.spec/knowledge/README.md` 的“设计流程失守回代”入口，要求布局、风格、prompt、Open Design、HTML 预览、AI 核图或实现骨架前必须先写启动自证：本轮规则来源、三条以上影响画面的规则结论、正式素材输入链和人工验收状态。缺任一项只能补证据或阻塞，不得继续旧稿微调或送验。
- [x] 3.1.2o 用户审核文字版饱和 UI 设计：已新增并更新 `docs/games/mage-wars/design/reference/step1-runtime-board-saturated-ui-design.md`，按规则页、素材矩阵、91 张学徒卡合同和 DiceThrone 骰子参考写出主界面必须交互元素、饱和交互状态、素材职责、状态读数自制 UI 裁定和设计稿继承约束；该条记录的是进入 Open Design 前的文字版审核状态。后续用户已批准进入 Open Design 候选生成，并已产出 v6；v6 用户人工批准仍待定。
- [x] 3.1.3 重做主 UI 视觉稿：旧 Open Design v1 / v2 / v3 / v4 均已保留为失败或历史候选；v5 虽通过但已被 v6 取代。已基于已批准文字版 UI 设计重做 Open Design artifact v6：`D:\codex-home\tools\open-design\.od\projects\mage-wars-ui-design\mage-wars-step1-runtime-board-v6.html`，渲染截图 `docs/games/mage-wars/design/generated/step1-runtime-board-opendesign-artifact-v6.png`；本轮确认未调用 `od media generate`、31 张正式素材均加载成功、状态板仍为 `reference-only`、饱和对象和己方 / 对手计划边界已补齐；AI 图面核验 `PASS 93/100`，审计见 `docs/games/mage-wars/design/generated/step1-runtime-board-opendesign-artifact-v6-audit.md`。当前允许打开给用户人工验收，但用户人工批准仍待定；未获批准前不得把 v6 当最终视觉依据进入高保真实现。
- [x] 3.2 新增 `src/games/mage-wars/` 骨架、manifest、i18n、thumbnail 和 domain 占位；已运行 `npm run generate:manifests`、`npx vitest run src/games/mage-wars`、新增 TS/TSX ESLint 和 OpenSpec 校验。
- [x] 3.3 建立领域对象模型、术语到事件映射、决策点清单和引擎能力缺口清单，见 `docs/games/mage-wars/rule/domain-modeling.md`。
- [x] 3.4.1 录入四名学徒法师统一属性、预设法术书组成、Workshop `CardID` 候选和别名裁定，不用占位卡牌冒充正式素材。
- [x] 3.4.2 继续录入逐卡完整效果文本、费用、派系、等级、目标规则和正式 atlas/frame 合同；已补 `docs/games/mage-wars/rule/apprentice-card-atlas-contract.md` 锁定 deck 源图、网格和 `CardID` frame 候选，并在 `temp/mage-wars/apprentice-card-crops/` 生成 91 张临时完整单卡核对图；四组同名多候选已按新手法术书实例裁定；`docs/games/mage-wars/rule/apprentice-card-field-contract.md` 已锁定学徒范围 deck `17`、`18`、`19`、`22`、`28`、`29`、`34`、`35`、`36`、`37` 的 91 张 S0 字段合同。已复制正式学徒法术 atlas 到 `public/assets/i18n/zh-CN/mage-wars/cards/spells/`，运行 `npm run compress:images -- public/assets/i18n/zh-CN/mage-wars`，生成 `public/assets/atlas-configs/mage-wars/apprentice-spell-atlases.json` 覆盖 91 个 frame，并更新本地 assets manifest；已新增运行时 atlas loader / `CardPreview` 注册与 `criticalImageResolver` 预加载，当前 Board 已消费正式竞技场、法师 atlas、学徒法术 atlas、卡背、行动 / 快速施法 token、攻击骰和来源锁定效果骰 UI；`npx vitest run src/games/mage-wars` 验证 15 tests passed；`e2e/mage-wars/foundation-board-runtime.e2e.ts` 已证明桌面真实入口本地素材消费和截图落盘；服务器主源与 Android 游戏素材包已于 2026-07-29 完成公开 URL / file-index / ZIP 回查，详见 `docs/games/mage-wars/design/generated/runtime-resource-chain-audit.md`。
- [x] 3.4.3 合 main 后按 `GameConfigPackage` 新规范复查数据与配置录入：已确认配置属于数据驱动录入的一部分，不能只查卡牌字段；当前法师 / 法术书、91 张法术 frame、法师 atlas、manifest、i18n、事件 / FX 映射等配置面可追溯，但仍分散在 TypeScript、atlas JSON、manifest、i18n 和运行时代码中。已更新 `openspec/changes/add-mage-wars-foundation/design.md` 与 `docs/games/mage-wars/intake/game-config-package-recheck-20260731.md`；严格 JSON 配置包、schema、表格审查和字段级修正提案标为后续独立 `add-mage-wars-config-package` 或等价 change，不混入本 foundation 合并。
- [x] 3.5 实现基础回合、计划、施法、移动、攻击、守卫、状态和胜负收口；已补 `src/games/mage-wars/__tests__/domain-flow.test.ts`，用真实 `executePipeline` 覆盖计划、聚魔、施法、移动、守卫、攻击伤害、行动 / 快速施法就绪状态、回合切换和胜负收口，并覆盖非当前玩家 / 非法目标 / 非邻接等负向校验。
- [x] 3.6 实现规则事件驱动的法术释放 FX；已新增 `src/games/mage-wars/ui/fxSetup.ts`、`fxRenderers.tsx`、`eventFxMapper.ts` 和 `useGameEvents.ts`，通过 `EventStreamSystem` 的 requiredSequence 消费 `MW_SPELL_CAST_RESOLVED`、`MW_ATTACK_DECLARED` 与 `DAMAGE_DEALT`，在 Board 接入 `FxLayer`；已补 `src/games/mage-wars/__tests__/event-fx-mapper.test.ts` 覆盖施法目标区、玩家目标和攻击 / 伤害映射。
- [x] 3.7 补单测、在线入口 E2E、桌面过程态截图和素材链验证；已新增 `e2e/mage-wars/foundation-board-runtime.e2e.ts`，真实入口 `/play/mage-wars` 的桌面截图 `test-results/evidence-screenshots/mage-wars/foundation-board-runtime/e2e-desktop-board.png` 已于 2026-08-01 02:24 +08:00 重拍并通过 AI 图面核验（PASS 93/100），且已打开到 PureRef 等待用户人工确认；移动横屏截图 `test-results/evidence-screenshots/mage-wars/foundation-board-runtime/e2e-mobile-landscape-board.png` 已重拍并通过真实入口 E2E。E2E 已新增几何断言，证明已计划法术与法术书卡面同尺寸，且右下全局悬浮圆不再压住已计划法术；证据记录到 `test-results/evidence-screenshots/mage-wars/foundation-board-runtime/evidence.md`、`docs/games/mage-wars/design/generated/runtime-board-implementation-audit.md` 与 `docs/games/mage-wars/design/generated/runtime-board-rule-object-coverage.md`；服务器资源主源已发布 34 个 `mage-wars/**/compressed/*.webp` 和 3 个运行时 JSON，代表对象远端 `200` 且大小 / SHA-256 与本地一致；Android `games/mage-wars.json`、file-index 和完整 ZIP 已发布并回查通过，file-index `files=37`、`sourceOrTemp=0`，详见 `docs/games/mage-wars/design/generated/runtime-resource-chain-audit.md`。
