# Design I/O Contracts

用于把模糊 UI 需求拆成设计声明和执行契约。只加载与当前任务相关的部分。

## 链路顺序

1. **理解规划**：把一句话需求变成目标、用户、状态、边界和验收。
2. **参考基线**：如果该 UI 有成熟答案，先写 reference baseline 和 gap matrix，再进入骨架。
3. **搭骨架**：确定信息区域、主视线、操作入口、密度和响应式去向。
4. **填充组件**：把业务语义映射到正确组件，不把不同职责混成同一种标签。
5. **细化工艺**：补空态、加载、错误、动效、层级、token 和视觉细节。
6. **评估回流**：按声明逐项检查，失败时回到对应环节。

## 设计声明模板

### `spec`

- 目标用户：
- 本页/本界面要完成的现实任务：
- 主路径：
- 必须覆盖状态：空态、加载态、错误态、权限态、等待态、完成态。
- 边界条件：
- 验收标准：

### `reference`（有成熟答案时必填）

- 成熟案例的第一眼信号：
- 行业固定对象关系：
- 必须覆盖的状态截图链：
- 可借鉴的视觉语法：
- 明确不能复制的版权素材：
- 当前实现差距矩阵：

### `exploration`（没有标准答案时必填）

- 不可变约束：
- 核心循环：
- 主要风险：
- 可借鉴类比：
- 待验证假设：
- 2-3 条设计方向及其设计轴：
- decision matrix：
- 胜出方向和放弃项：

### `domain`

- 业务对象：
- 对象状态：
- 风险/优先级/成功失败语义：
- 敏感信息或危险动作：
- 禁止混用的语义：

### `design`

- 视觉气质：
- 页面密度：
- 主视觉素材或品牌来源：
- token 使用规则：
- 颜色、字体、间距、圆角、阴影、动效时长：
- 禁止 hardcode 项：

### `components`

| 信息/动作 | 现实语义 | 组件/承载 | 禁止替代 |
| --- | --- | --- | --- |
|  |  |  |  |

常见边界：

- Badge 表达状态、计数、风险。
- Tag 表达分类、类型、环境。
- Drawer 承载复杂详情。
- Dialog 承载需要确认或中断流程的动作。
- 主操作按钮不能代替对象选择；对象选择应回到对象本体或其投影区域。

### `craft`

- 层级是否平均化：
- 是否出现模板味/AI 味：
- 空/加载/错误是否稳定：
- 动效是否有目的：
- 文案是否复述画面已有信号：
- 常驻 UI 是否塞入解释正文：

### `template`

- 页面/界面区域：
- 主视线：
- 主要交互入口：
- 辅助信息去向：
- 最大承载量：
- 桌面与移动的对应矩阵：

## 执行契约模板

- 读取时机：
- 写入落点：
- 下游使用者：
- 实现必须守住的结构合同：
- 可降级的概念/材质：
- 验收入口：
- evaluator：
 - reference baseline / gap matrix（如适用）：

## 最小通过标准

- 用户目标、主路径、边界态和验收口径同时存在。
- 有成熟答案的 UI 已先建立参考基线和差距矩阵。
- 没有标准答案的 UI 已先建立探索基线、不同设计轴和收敛矩阵。
- 业务语义有组件映射，不靠视觉外观猜职责。
- 视觉取值来自 token、设计系统或明确记录的缺口。
- 骨架说明主视线和主操作入口，不只是组件清单。
- evaluator 能指出问题回到哪份声明。
