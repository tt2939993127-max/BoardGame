# 测试模式（调试面板）

测试模式用于**快速联机测试**：执行一次行动后自动切换到另一位玩家视角。

## 入口

- 调试面板（右下角工具按钮）
- `⚙️ 系统` 标签页中的测试模式开关

## 行为

- **开启**：执行任意 move 后自动切换视角（P0 ⇄ P1）
- **关闭**：视角保持不变，可手动切换

## 状态

- 开关状态会持久化到本地存储（键：`debug_testMode`）

## 限制

- 仅面向开发调试使用
- 主要适用于 2 人对局
