# Mage Wars 标注图饱和布局 v49 出图前硬回执

> 状态：`preflight-ready / open-design-artifact-only / media-generate-forbidden / human-review-not-allowed / implementation-blocked / mobile-blocked`。本轮只修复 v48 的“已计划法术与已选法术重复实体”问题，不进入真实 Board/UI、真实运行页 E2E 或移动端适配。

## 本轮问题锁定

| 项 | 当前锁定 |
| --- | --- |
| 问题对象 | `mage-wars-step1-annotated-saturated-v48.html` 的底部已选法术复制卡与右下已计划法术槽 |
| 真相来源 | 用户人工复核反馈、`step1-runtime-board-saturated-ui-design.md` 的“当前来源对象 / 已计划法术 / 法术书”语义、v48 原图 |
| 目标入口 / 环境 | Open Design artifact 代码设计稿 `mage-wars-step1-annotated-saturated-v49.html` |
| 验收口径 | 同一张 `火球术` 只能由一个可见卡牌主体承接；AI 图面核验通过后才允许人工验收 |

## 规则到画面结论

| 规则 / 设计结论 | 影响主体 | v49 设计决策 |
| --- | --- | --- |
| `火球术` 已经是己方 2 张已计划法术之一 | 右下已计划槽 | 保留右下已计划槽中的 `火球术`，并把这张真实计划卡放大 / 高亮为当前来源 |
| “当前来源”是已计划槽里被选中的同一张牌，不是另一张新实体 | 底部原“已选法术”大卡 | 删除底部复制卡，只保留短标签 `当前来源` 贴在右下已计划 `火球术` 上 |
| 执行态法术书不是当前来源主体 | 底部法术书浏览 | 法术书列表不再重复已计划 `火球术`，改显示其它可浏览法术，并把数量改为 `法术书 28` |
| 当前动作仍是 `火球术 -> B2 西锁骑士` | 路径线和目标摘要 | 施法路径从右下已计划 `火球术` 指向 B2 目标，目标摘要保持 `西锁骑士 / B2` |

## 禁止项

- 不调用 `od media generate`，不走图片模型生图。
- 不写真实 Board/UI，不启动真实运行页，不跑实现 E2E，不做移动端。
- 不再创建底部“已选法术”大卡，不让同一张已计划 `火球术` 出现为两个物理实体。
- 不引入 `手牌`、`hand`、`opponent-hand` 或常驻 `确认 / 执行 / 取消`。

## 人工验收状态

- 当前 v49 只能进入截图和 AI 图面核验。
- AI 图面核验通过前，仍为 `human-review-not-allowed`。
- 若 AI 图面发现 `火球术` 仍被复制成两个来源主体、目标链断开、骰子 / token 缺失或出现规则外术语，本稿直接降为 `REVISE`，不得打开给用户。
