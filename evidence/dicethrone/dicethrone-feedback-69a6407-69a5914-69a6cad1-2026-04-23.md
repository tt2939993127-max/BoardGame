# Dicethrone 反馈 69a6407 / 69a5914 / 69a6cad1 收口记录

日期：2026-04-23

## 69a6407dbd494244e5a2a622 闪避投掷之后卡住

- 是否复现：已复现，但只在缺少完整 defensiveRoll 上下文的注入场景里会稳定卡在 `defensiveRoll`；补齐真实 `defenseAbilityId` 后，当前分支单测与 E2E 都通过，未再复现用户反馈中的卡死。
- 根因：
  - 历史反馈语义对应的问题点是：`USE_TOKEN(evasive)` 成功后，`TOKEN_RESPONSE_CLOSED` 已清空 `pendingDamage`，但阶段没有继续从 `defensiveRoll` 收口到 `main2`。
  - 本轮排查确认，真正的收口链依赖完整 defensiveRoll 门禁上下文，尤其是 `pendingAttack.defenseAbilityId`。没有这个字段时，自动推进会被 `canAdvancePhase(defensiveRoll)` 拒绝，看起来像“闪避后卡住”。
- 修复点：
  - 补了流程级回归，覆盖 `shadow_thief -> moon_elf -> evasive` 的真实命令链，要求 `USE_TOKEN` 成功后自动进入 `main2`。
  - 补了现有 E2E 文件中的 UI 回归，固定闪避成功骰面，验证“响应弹窗出现 -> 点击使用 -> 收口到 main2”。
- 验证命令：
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/dicethrone/__tests__/flow.test.ts -t "闪避投掷成功后应自动收口到 main2，而不是卡在 defensiveRoll" --configLoader native --maxWorkers 1`
  - `npm run test:e2e:ci:file -- e2e/dicethrone/dicethrone-token-response-window.e2e.ts "月精灵闪避成功后应自动收口到 main2，不再卡在 defensiveRoll"`
- 关键截图绝对路径：
  - `D:\gongzuo\webgame\BoardGame\test-results\evidence-screenshots\dicethrone\dicethrone-token-response-window.e2e\月精灵闪避成功后应自动收口到-main2，不再卡在-defensiveRoll\moon-elf-evasive-before-use.png`
  - `D:\gongzuo\webgame\BoardGame\test-results\evidence-screenshots\dicethrone\dicethrone-token-response-window.e2e\月精灵闪避成功后应自动收口到-main2，不再卡在-defensiveRoll\moon-elf-evasive-after-use.png`
- 看图结论：
  - `before-use`：页面中央出现“响应（防御方）”弹窗，原始伤害与当前伤害均为 5，`闪避` Token 与“使用/跳过”按钮可见，说明问题场景已真实进入响应窗口。
  - `after-use`：响应弹窗消失，左侧阶段高亮切到 `6. 主要阶段(2)`，月精灵血量仍为 50，说明闪避成功后没有停留在 `defensiveRoll`，流程已收口。

## 69a5914ebd494244e5a29f92 锁定骰子被“弹一手”影响异常

- 是否复现：当前分支未复现。
- 根因：
  - 该反馈对应的历史问题是 UI/交互层把可修改骰子错误限制为 `!d.isKept`，没有区分 `targetOpponentDice=true` 的对手骰池场景。
  - 领域层本身允许修改对手已锁定骰子；异常发生在前端选择阶段，而不是命令执行阶段。
- 修复点：
  - 本轮未再改实现；当前分支已有对应行为修复。
  - 重新运行并保留现有回归，确认：
    - 对手锁定骰子时，`弹一手` 仍可修改该骰子；
    - 响应窗口交互的 `targetOpponentDice=true` 仍然成立。
- 验证命令：
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/dicethrone/__tests__/card-flick-locked-dice.test.ts src/games/dicethrone/__tests__/flick-response-debug.test.ts --configLoader native --maxWorkers 1`
- 关键截图绝对路径：
  - 本轮未跑 E2E，无截图。

## 69a6cad10b03a64d4e0c89b1 火法小顺子二级技能无法触发焚灭

- 是否复现：当前分支未复现。
- 根因：
  - 该反馈对应的是 `Hot Streak II` 的变体覆盖不足风险：除了小顺子本身，还要覆盖 `2火 + 2爆发` 触发 `incinerate`，以及两类条件同时满足时的优先级。
  - 当前分支逻辑已正确，但此前缺少直接贴反馈语义的回归，容易让问题再次回归而无测试报警。
- 修复点：
  - 在现有 `pyromancer-hot-streak-2-bug.test.ts` 中补了两条回归：
    - `2火 + 2爆发` 时出现 `incinerate`
    - `小顺子 + 焚灭条件` 同时满足时，`incinerate` 优先级高于 `fiery-combo-2`
- 验证命令：
  - `node scripts/infra/vitest-cli-safe.mjs run src/games/dicethrone/__tests__/pyromancer-hot-streak-2-bug.test.ts --configLoader native --maxWorkers 1`
- 关键截图绝对路径：
  - 本轮未跑 E2E，无截图。
