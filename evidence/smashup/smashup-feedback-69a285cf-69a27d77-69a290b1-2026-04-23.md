# SmashUp 反馈 lane 收口证据（69a285cf / 69a27d77 / 69a290b1）

## 范围

- 反馈 `69a285cf17d6c588726804fd`
- 反馈 `69a27d7717d6c588726803a3`
- 反馈 `69a290b117d6c588726806a5`

仅检查并补强 `SmashUp` 相关实现现状、测试与证据；未触碰 `Dice Throne` 或其他无关模块。

## 共因判断

- `69a27d7717d6c588726803a3` 与 `69a290b117d6c588726806a5` 共因高度一致：
  - 都落在 `afterScoring` / 延迟清场事件 / 替换基地后续动作 / `scoreBases` 收口链路。
  - 现工作树里的共因修复已经体现在 `scoreBases` scoring session、`_deferredPostScoringEvents` 传递与补发、以及 afterScoring 统一响应窗口收口。
- `69a285cf17d6c588726804fd` 症状不同：
  - 更接近 `Igor` 的销毁触发只应消费一次，避免“被消灭”与“离开基地/弃牌”双重触发导致重复 `+1`。
  - 它不是和前两条完全同一根因，但同属“事件链重复消费/重复处理”类回归。

## 复现结论

### 69a285cf17d6c588726804fd

- 当前工作树下，未复现“科学小怪蛋被消灭后触发两次 +1”。
- 补强后的回归测试证明：
  - 只会出现一次 `su:power_counter_added`
  - `payload.reason === 'frankenstein_igor'`
  - 最终只给目标随从加 1 个指示物

### 69a27d7717d6c588726803a3

- 当前工作树下，未复现“海盗王移动基地后计分卡死”。
- 补强后的全链路测试证明：
  - `pirate_king` 先在 `beforeScoring` 移入托尔图加
  - 托尔图加与大副 afterScoring 链结束后，状态会离开 `scoreBases`
  - 不残留 `smashupReactionSession` / `responseWindow` / 交互队列

### 69a290b117d6c588726806a5

- 当前工作树下，未复现“基地计分后有效果时卡死”。
- 以 `base_greenhouse` 为代表补了真实 afterScoring 链路测试，证明：
  - 基地计分后可从牌库选择随从
  - `BASE_CLEARED` / `BASE_REPLACED` 只补发一次
  - 温室效果落地后状态离开 `scoreBases/afterScoring`
  - 后续允许正常进入下一阶段合法交互（本例为 `base_secret_garden` 带来的 `smashup_immediate_extra_minion`）

## 本轮改动

- 仅补现有测试文件，不新增测试文件，不改 SmashUp 实现层逻辑。
- 说明：
  - 现工作树里已存在与共因修复相关的未提交实现改动；本轮避免覆盖，只补反馈级回归覆盖。

## 改动文件

- `D:\gongzuo\webgame\BoardGame\src\games\smashup\__tests__\igor-two-igors-one-destroyed.test.ts`
- `D:\gongzuo\webgame\BoardGame\src\games\smashup\__tests__\afterScoring-rescoring.test.ts`
- `D:\gongzuo\webgame\BoardGame\src\games\smashup\__tests__\multi-base-afterscoring-bug.test.ts`
- `D:\gongzuo\webgame\BoardGame\evidence\smashup\smashup-feedback-69a285cf-69a27d77-69a290b1-2026-04-23.md`

## 验证命令

以下命令均已实跑通过：

```powershell
node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/igor-two-igors-one-destroyed.test.ts --configLoader native --pool threads --no-file-parallelism --maxWorkers 1
node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/afterScoring-rescoring.test.ts --configLoader native --pool threads --no-file-parallelism --maxWorkers 1
node scripts/infra/vitest-cli-safe.mjs run src/games/smashup/__tests__/multi-base-afterscoring-bug.test.ts --configLoader native --pool threads --no-file-parallelism --maxWorkers 1
```

## 结论

- `69a285cf17d6c588726804fd`：当前工作树已不复现，可转 `resolved`
- `69a27d7717d6c588726803a3`：当前工作树已不复现，可转 `resolved`
- `69a290b117d6c588726806a5`：当前工作树已不复现，可转 `resolved`

## 剩余风险

- 本轮未跑 SmashUp UI/E2E，因为改动仅为回归测试补强，且未新增前端交互实现。
- 若你要把这三条作为线上 closeout 证据提交，建议直接引用本文件与上述 3 个测试文件。
