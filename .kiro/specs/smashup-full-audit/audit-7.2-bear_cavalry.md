# 审查 7.2：黑熊骑兵（Bear Cavalry）派系多步骤能力

> 审查日期：2026-02-14
> 权威描述来源：`public/locales/zh-CN/game-smashup.json`

## 审查汇总

| 指标 | 数值 |
|------|------|
| 总交互链数 | 12 |
| ✅ 通过 | 8 |
| ⚠️ 语义偏差 | 3 |
| ❌ 缺失实现 | 1 |
| 📝 测试缺失 | 2 |
| 通过率 | 66.7% |

## 严重问题清单

| 优先级 | 交互链 | 问题 | 维度 |
|--------|--------|------|------|
| P0 | bear_cavalry_commission | 描述"然后将该随从所在基地上另一位玩家的一张随从移动到其他基地"第二步完全未实现 | D2 |
| P1 | bear_cavalry_bear_hug | "平局则由拥有者选择"未实现——平局时自动选第一个找到的，无玩家交互 | D5 |
| P2 | bear_cavalry_general_ivan | 描述"你的随从不能被消灭"无"被对手"限定，但实现只保护不被对手消灭 | D1 |
| P2 | bear_cavalry_bear_necessities | 描述"消灭随从或在基地上打出的一张战术"无敌我限定，但实现只选对手目标 | D1 |

---

## 审查矩阵

### 交互链 1：bear_cavalry_general_ivan — 己方随从不能被消灭（持续保护）

**权威描述**：「持续：你的随从不能被消灭。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — `abilityTags: ['ongoing']` |
| 注册层 | ✅ | `registerProtection('bear_cavalry_general_ivan', 'destroy', bearCavalryGeneralIvanChecker)` |
| 执行层 | ⚠️ | ① 遍历所有基地找 ivan，检查 `ivan.controller === ctx.targetMinion.controller` ✅。② 排除 ivan 自身 `ctx.targetMinion.uid === ivan.uid` 时 `continue` ✅（"你的随从"不含自身合理）。③ **只在 `ctx.sourcePlayerId !== ctx.targetMinion.controller` 时保护**——描述"你的随从不能被消灭"无"被对手"限定词，但实现只保护不被对手消灭，己方效果（如自爆）可消灭己方随从（D1 语义保真：多加了"仅对手"限制）。④ 多 ivan 场景：循环遍历所有基地，找到一个即返回 true ✅ |
| 状态层 | N/A | 保护拦截 |
| 验证层 | N/A | |
| UI 层 | N/A | |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newOngoingAbilities.test.ts` 有三个测试：保护己方、不保护自身、不保护对手 |

### 交互链 2：bear_cavalry_polar_commando — 唯一时+2力量+不可消灭（持续）

**权威描述**：「持续：如果这个随从是所在基地上唯一的你的随从，则它获得力量+2并且不能被消灭。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — `abilityTags: ['ongoing']` |
| 注册层 | ✅ | `registerProtection('bear_cavalry_polar_commando', 'destroy', ...)` + `registerPowerModifier('bear_cavalry_polar_commando', ...)` |
| 执行层 | ✅ | ① 保护检查：`defId === 'bear_cavalry_polar_commando'` + 基地上己方随从数 === 1 ✅。② 力量修正：同条件 +2 ✅。③ 两个效果条件一致 ✅ |
| 状态层 | ✅ | 通过 `getEffectivePower` 和保护系统 |
| 验证层 | N/A | |
| UI 层 | N/A | |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newOngoingAbilities.test.ts` 有三个测试：唯一时不可消灭、有其他随从时可消灭、唯一时+2力量 |

### 交互链 3：bear_cavalry_bear_cavalry — 移动对手随从到另一基地（onPlay）

**权威描述**：「将其他玩家在本随从所在的基地的一个随从移动到另一个基地。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — `abilityTags: ['onPlay']` |
| 注册层 | ✅ | `registerAbility('bear_cavalry_bear_cavalry', 'onPlay', bearCavalryBearCavalryAbility)` |
| 执行层 | ✅ | ① 过滤本基地对手随从 `m.controller !== ctx.playerId` ✅。② 排除自身 `m.uid !== ctx.cardUid` ✅。③ 两步交互：选随从→选目标基地 ✅。④ 无对手随从时静默返回 ✅。⑤ handler 发射 `moveMinion` 事件 ✅ |
| 状态层 | ✅ | `MINION_MOVED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | ✅ | 两步链式 Prompt |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newFactionAbilities.test.ts` 有正向（创建 Prompt）和负向（无对手随从无交互）测试 |

### 交互链 4：bear_cavalry_cub_scout — 对手弱随从移入时消灭（持续触发）

**权威描述**：「持续：当其他玩家的随从移动到本随从所在的基地之后，如果它的力量低于本随从，则消灭它。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — `abilityTags: ['ongoing']` |
| 注册层 | ✅ | `registerTrigger('bear_cavalry_cub_scout', 'onMinionMoved', bearCavalryCubScoutTrigger)` |
| 执行层 | ✅ | ① 找目标基地上所有 cub_scout ✅。② 排除同控制者 `scout.controller === movedMinion.controller` ✅。③ 力量比较 `movedPower < scoutPower`（严格小于）与描述"低于"一致 ✅。④ 找到第一个匹配的 scout 后 break ✅ |
| 状态层 | ✅ | `MINION_DESTROYED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | N/A | 自动触发 |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newOngoingAbilities.test.ts` 有正向（力量低被消灭）和负向（力量不低不消灭）测试 |

### 交互链 5：bear_cavalry_bear_hug — 每位对手消灭最弱随从

**权威描述**：「每位其他玩家均消灭自己战斗力最低的一张随从。（平局则由拥有者选择）」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — 行动卡，`subtype: 'standard'` |
| 注册层 | ✅ | `registerAbility('bear_cavalry_bear_hug', 'onPlay', bearCavalryBearHug)` |
| 执行层 | ⚠️ | ① 遍历所有对手，找每位对手全场最弱随从 ✅。② 力量比较用 `getMinionPower` 考虑修正 ✅。③ **"平局则由拥有者选择"未实现**——代码遍历时只保留第一个最弱随从（`power < weakest.power`），平局时不更新，自动选第一个找到的，无玩家选择交互（D5 交互完整性违规）。④ 无随从的对手跳过 ✅ |
| 状态层 | ✅ | `MINION_DESTROYED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | ⚠️ | 缺少平局时的拥有者选择交互 |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `expansionAbilities.test.ts` 有多场景测试（多对手、无随从、多基地），但无平局选择测试 |

### 交互链 6：bear_cavalry_commission — 额外随从+移动对手随从

**权威描述**：「打出一张额外随从。然后将该随从所在基地上另一位玩家的一张随从移动到其他基地。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — `abilityTags: ['extra']` |
| 注册层 | ✅ | `registerAbility('bear_cavalry_commission', 'onPlay', bearCavalryCommission)` |
| 执行层 | ❌ | ① `grantExtraMinion` 授予额外随从额度 ✅。② **"然后将该随从所在基地上另一位玩家的一张随从移动到其他基地"完全未实现**——函数只返回 `grantExtraMinion` 事件，无后续移动交互。描述明确有两步效果，第二步缺失（D2 边界完整违规：缺少完整效果链） |
| 状态层 | ✅ | `LIMIT_MODIFIED` 被 reduce 处理（仅第一步） |
| 验证层 | N/A | |
| UI 层 | ❌ | 缺少第二步移动交互 |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ⚠️ | `expansionAbilities.test.ts` 只测试了额度授予，未测试移动效果 |

### 交互链 7：bear_cavalry_youre_screwed — 三步移动对手随从

**权威描述**：「选择一个有你随从的基地，将另一位玩家在这里的一张随从移动到其他基地。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — 行动卡，`subtype: 'standard'` |
| 注册层 | ✅ | `registerAbility('bear_cavalry_youre_screwed', 'onPlay', bearCavalryYoureScrewed)` |
| 执行层 | ✅ | ① 过滤有己方随从且有对手随从的基地 ✅。② 三步链式交互：选基地→选对手随从→选目标基地 ✅。③ 无候选基地时静默返回 ✅。④ handler 链正确传递 continuationContext ✅ |
| 状态层 | ✅ | `MINION_MOVED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | ✅ | 三步链式 Prompt |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newFactionAbilities.test.ts` 有正向和负向测试 |

### 交互链 8：bear_cavalry_bear_rides_you — 移动己方随从

**权威描述**：「将你的一张随从从移动到其他基地。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — 行动卡，`subtype: 'standard'` |
| 注册层 | ✅ | `registerAbility('bear_cavalry_bear_rides_you', 'onPlay', bearCavalryBearRidesYou)` |
| 执行层 | ✅ | ① 收集所有基地上己方随从 ✅。② 两步交互：选随从→选目标基地 ✅。③ 无己方随从时静默返回 ✅。④ handler 发射 `moveMinion` 事件 ✅ |
| 状态层 | ✅ | `MINION_MOVED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | ✅ | 两步链式 Prompt |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newFactionAbilities.test.ts` 有正向和负向测试 |

### 交互链 9：bear_cavalry_youre_pretty_much_borscht — 移动基地上所有对手随从

**权威描述**：「选择一个有你随从的基地，将其他玩家在该基地的所有随从移动到其他基地。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — 行动卡，`subtype: 'standard'` |
| 注册层 | ✅ | `registerAbility('bear_cavalry_youre_pretty_much_borscht', 'onPlay', bearCavalryYourePrettyMuchBorscht)` |
| 执行层 | ✅ | ① 过滤有己方随从且有对手随从的基地 ✅。② 两步交互：选来源基地→选目标基地 ✅。③ handler 批量移动所有对手随从 ✅。④ 无候选基地时静默返回 ✅ |
| 状态层 | ✅ | 多个 `MINION_MOVED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | ✅ | 两步链式 Prompt |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newFactionAbilities.test.ts` 有正向测试 |

### 交互链 10：bear_cavalry_bear_necessities — 消灭随从或行动卡

**权威描述**：「消灭随从或在基地上打出的一张战术。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — 行动卡，`subtype: 'standard'` |
| 注册层 | ✅ | `registerAbility('bear_cavalry_bear_necessities', 'onPlay', bearCavalryBearNecessities)` |
| 执行层 | ⚠️ | ① 收集随从目标：`m.controller === ctx.playerId` 时 `continue`（跳过己方随从）——描述"消灭随从"无敌我限定（D1 语义保真：多加了"仅对手"过滤）。② 收集行动卡目标：`o.ownerId !== ctx.playerId`（跳过己方行动卡）——同上。③ 使用 `resolveOrPrompt` 数据驱动，单候选自动执行 ✅。④ handler 正确区分随从/行动卡类型 ✅。⑤ 行动卡消灭发射 `ONGOING_DETACHED` 事件 ✅ |
| 状态层 | ✅ | `MINION_DESTROYED` / `ONGOING_DETACHED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | ✅ | `resolveOrPrompt` 自动处理 |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newFactionAbilities.test.ts` 有多场景测试（多目标 Prompt、单目标自动执行、行动卡消灭、无目标） |

### 交互链 11：bear_cavalry_superiority — 多重保护（持续）

**权威描述**：「打出到基地上。持续：你在这里的随从不能被其他玩家的卡牌能力消灭、移动或者返回到你的手牌或牌库。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — `abilityTags: ['ongoing']`, `subtype: 'ongoing'` |
| 注册层 | ✅ | 三重注册：`registerProtection('bear_cavalry_superiority', 'destroy', ...)` + `'move'` + `'affect'` |
| 执行层 | ✅ | ① 检查 `ctx.sourcePlayerId === ctx.targetMinion.controller` 时不保护（己方效果不拦截）✅。② 检查基地上有该 ongoing 且 `ownerId === ctx.targetMinion.controller` ✅。③ 三种保护类型统一用同一个 checker ✅ |
| 状态层 | N/A | 保护拦截 |
| 验证层 | N/A | |
| UI 层 | N/A | |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newOngoingAbilities.test.ts` 有保护己方和不保护对手测试 |

### 交互链 12：bear_cavalry_high_ground — 消灭移入的对手随从（持续触发）

**权威描述**：「打出到基地上。持续：如果这里有你的随从，则消灭其他玩家移动到这里的任何随从。」

| 层 | 状态 | 证据 |
|----|------|------|
| 定义层 | ✅ | `data/factions/bear_cavalry.ts` — `abilityTags: ['ongoing']`, `subtype: 'ongoing'` |
| 注册层 | ✅ | `registerTrigger('bear_cavalry_high_ground', 'onMinionMoved', bearCavalryHighGroundTrigger)` |
| 执行层 | ✅ | ① 找目标基地上的 high_ground ongoing ✅。② 排除同控制者 `ongoing.ownerId === movedMinion.controller` ✅。③ 检查 owner 在基地上有随从 `ownerHasMinion` ✅。④ 发射 `destroyMinion` 事件 ✅ |
| 状态层 | ✅ | `MINION_DESTROYED` 被 reduce 处理 |
| 验证层 | N/A | |
| UI 层 | N/A | 自动触发 |
| i18n 层 | ✅ | 双语条目存在 |
| 测试层 | ✅ | `newOngoingAbilities.test.ts` 有正向测试 |

---

## 交叉影响备注

### bear_cavalry_commission 第二步缺失

这是 P0 问题。描述明确说"打出一张额外随从。然后将该随从所在基地上另一位玩家的一张随从移动到其他基地"，但实现只有 `grantExtraMinion`，完全没有第二步移动逻辑。修复方案：需要在额外随从打出后触发移动交互——这比较复杂，因为需要追踪"该随从"打到了哪个基地，然后在该基地选择对手随从移动。可能需要用 FlowHooks 或 onPlay 后续链来实现。

### bear_cavalry_bear_hug 平局选择

描述明确说"平局则由拥有者选择"，但实现中平局时自动选第一个找到的随从。修复方案：当某位对手有多个最弱随从（力量相同）时，应创建 Prompt 让该对手选择消灭哪一个。

### bear_cavalry_general_ivan 与 bear_cavalry_bear_necessities 目标范围

两者的描述都没有"对手"限定词，但实现都只针对对手。在 SmashUp 中，消灭自己的随从/行动卡有时是有意义的策略（如触发 onDestroy 效果），此限制可能影响策略深度。不过这也可能是设计意图——需要与规则原文核对。
